package use.exanys.suggester.components

import kotlinx.serialization.*
import kotlinx.serialization.json.JSON

class WordsCounter {
    fun countAllWordInDocs(docs: List<Any>){
        var arrayOfWords: MutableList<String>
        var position: Number = 1
        for (doc in docs){

        }
    }
}